# Environment / secrets
.env
.env.local
*.token

# Process docs live in Confluence, not here. These are local working state.
CLAUDE.md
HANDOFF.md
MARCUS-DECISIONS.md
docs/
.scratch/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv
.uv/

# Claude Code local machine state
.claude/settings.local.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# A deliberate local MLflow backend (MLFLOW_TRACKING_URI=sqlite:///mlflow.db). The
# harness refuses to run with the variable unset, so neither of these should appear
# by accident — an untracked file here would make every run report git_dirty=true.
mlflow.db
mlruns/
